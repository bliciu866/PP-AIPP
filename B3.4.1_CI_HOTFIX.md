# B3.4.1 CI Hotfix

This hotfix updates one stale regression assertion in
`tests/test_export_engine.py` from `3.0.0b10` to `3.0.0b11`.

The B3.4 engine, book layout and application version are unchanged. After
copying the hotfix into the repository, commit with:

`Fix B3.4 export manifest version test`

Push to `main` and wait for both GitHub Actions workflows to turn green.
